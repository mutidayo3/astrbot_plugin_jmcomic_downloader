{
  "transfer_mode": {
    "type": "string",
    "default": "auto",
    "description": "文件传输模式",
    "hint": "auto=自动检测(推荐), local=本地路径(非Docker使用), docker=HTTP服务器(Docker跨容器必需)",
    "options": ["auto", "local", "docker"]
  },
  "max_cache_count": {
    "type": "int",
    "default": 20,
    "minimum": 0,
    "maximum": 500,
    "description": "PDF 缓存数量上限",
    "hint": "0 表示不缓存，默认 20 本，按 LRU 淘汰"
  },
  "max_workers": {
    "type": "int",
    "default": 4,
    "minimum": 1,
    "maximum": 8,
    "description": "下载线程数",
    "hint": "建议根据网络状况调整，范围 1~8"
  },
  "image_format": {
    "type": "string",
    "default": "webp",
    "description": "下载图片格式",
    "hint": "选择从 JMComic 下载的图片原始格式。强烈建议 jpg：img2pdf 能把 JPEG 原字节直接嵌入 PDF，而 webp/png 必须解码成无损位图，实测同一本子 webp 生成 649 MB、jpg 仅 136 MB，且转换快上百倍",
    "options": ["webp", "jpg", "png"]
  },
  "download_timeout": {
    "type": "int",
    "default": 300,
    "description": "下载超时时间（秒）",
    "hint": "单本下载的最大等待时间，超时会自动终止任务"
  },
  "auto_cleanup": {
    "type": "bool",
    "default": true,
    "description": "自动清理临时目录",
    "hint": "发送完成后是否自动删除下载的原始图片文件夹"
  },
  "pdf_resolution": {
    "type": "float",
    "default": 150.0,
    "description": "PDF 分辨率 DPI",
    "hint": "控制 PDF 页面尺寸（DPI 越高页面越小、印刷越精细）。注意：不会重新编码图片，因此不影响文件大小——想减小体积请把 image_format 设为 jpg"
  },
  "file_server_port": {
    "type": "int",
    "default": 18790,
    "description": "HTTP 文件服务器端口",
    "hint": "Docker 模式下需将此端口映射到宿主机"
  },
  "file_server_base_url": {
    "type": "string",
    "default": "",
    "description": "文件服务器外网地址",
    "hint": "如 http://172.17.0.1:18790。Docker 模式下必填或留空自动推断"
  },
  "enable_zip": {
    "type": "bool",
    "default": false,
    "description": "启用 ZIP 打包发送",
    "hint": "将 PDF 打包为 ZIP（仅存储模式，不额外压缩），可防止平台在线扫描上传文件。支持可选 AES-256 加密"
  },
  "zip_password": {
    "type": "string",
    "default": "",
    "description": "ZIP 压缩包密码",
    "hint": "留空则不加密，建议使用强密码"
  },
  "max_cache_size_mb": {
    "type": "int",
    "default": 200,
    "minimum": 0,
    "maximum": 10000,
    "description": "缓存文件大小上限(MB)",
    "hint": "超过此大小的 PDF 不存入缓存，避免占用过多磁盘空间。0 表示不限制"
  },
  "rate_limit_window": {
    "type": "int",
    "default": 300,
    "minimum": 0,
    "maximum": 86400,
    "description": "重复请求限频窗口(秒)",
    "hint": "同一聊天中同一本子在设定时间内只允许获取一次。0 表示不限制，建议 300~600"
  },
  "max_image_count": {
    "type": "int",
    "default": 500,
    "minimum": 0,
    "maximum": 10000,
    "description": "图片数量上限",
    "hint": "漫画图片数量超过此值时拒绝下载，防止超大本子耗尽资源。0 表示不限制"
  },
  "debug_log": {
    "type": "bool",
    "default": false,
    "description": "启用调试日志",
    "hint": "开启后输出详尽的运行日志（缓存命中、下载参数、文件操作等），方便排查问题。正常运行时建议关闭"
  },
  "max_concurrent": {
    "type": "int",
    "default": 1,
    "minimum": 1,
    "maximum": 4,
    "description": "同时下载的最大并发数",
    "hint": "限制同时处理的下载请求数量，防止资源耗尽。设为 1 时严格按请求顺序处理，大于 1 时允许并发但仍按顺序放行"
  },
  "upload_retry": {
    "type": "int",
    "default": 3,
    "minimum": 1,
    "maximum": 10,
    "description": "文件上传重试次数",
    "hint": "QQ 的 Highway 传输通道会在随机位置中断，大文件尤其明显（与文件大小无关）。此值为总尝试次数，失败后按 3s/6s/12s 退避重试。设为 1 表示不重试"
  },
  "auto_recall": {
    "type": "bool",
    "default": true,
    "description": "自动撤回状态消息",
    "hint": "开启后，排队提示、进度更新等文字消息会在发送 30 秒后自动撤回，群聊中最终只留下文件。错误消息和密码提示不会被撤回"
  }
}